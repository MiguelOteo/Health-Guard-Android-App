<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="datePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/orange_secondary</item>
        <!--selected Item color-->
        <item name="colorControlActivated">@color/orange_main</item>
        <!-- Highlight item color-->
        <item name="colorControlHighlight">@color/orange_secondary</item>
        <!--Calender Background color -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Ok Cancel Color-->
        <item name="android:textColor">@color/orange_main</item>
        <!-- Week TextColor-->
        <item name="android:textColorSecondary">@color/orange_secondary</item>
        <!-- Calender Number color arrow color (< >) -->
        <item name="android:textColorPrimary">@color/orange_secondary</item>
        <!--day , month-->
        <item name="android:textColorPrimaryInverse">@color/white</item>
        <!-- year-->
        <item name="android:textColorSecondaryInverse">@color/white</item>

        <!-- arrow bordercolor(<>)
        <item name="android:selectableItemBackgroundBorderless">@color/orange</item>
        -->
    </style>
</resources>